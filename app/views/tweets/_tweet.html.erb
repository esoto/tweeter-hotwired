<%= turbo_frame_tag dom_id(tweet) do %>
  <div id="<%= dom_id tweet %>" class="my-10 p-6 max-w-sm mx-auto bg-gray-100 rounded-xl shadow-lg flex items-center space-x-4">
    <p>
      <strong>Body:</strong>
      <%= tweet.body %>
    </p>

  </div>
<% end %>